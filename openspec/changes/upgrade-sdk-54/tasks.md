# Tasks

- [ ] Update `package.json` to use `expo@^54.0.0` and `react-native` compatible version. <!-- id: update-deps -->
- [ ] Run `npx expo install --fix` to align all dependencies. <!-- id: install-fix -->
- [ ] Verify application startup with `npx expo start`. <!-- id: verify-start -->
- [ ] Validation check of key features in Expo Go. <!-- id: manual-check -->
