# Tasks

- [x] Update `package.json` to use `expo@^54.0.0` and `react-native` compatible version. <!-- id: update-deps -->
- [x] Run `npx expo install --fix` to align all dependencies. <!-- id: install-fix -->
- [x] Migrate `components/ui/Audio.tsx` from `expo-av` to `expo-audio`. <!-- id: migrate-audio -->
- [x] Verify application startup with `npx expo start`. <!-- id: verify-start -->
- [ ] Validation check of key features in Expo Go. <!-- id: manual-check -->
